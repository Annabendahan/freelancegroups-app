<div class="form-group">
<%= simple_form_for [@team, @event] do |f| %>

  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :location %>
  <%= f.input :date %>
  <%= f.button :submit, class: 'btn ' %> </div>
<% end %>
</div>
